sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# Replace specific brain component endings at the end of a string with the corresponding strings prepended in the "Anatomy" column
sheetfromtxt$Anatomy <- sub("^(.*)MES\\+D\\+S$", "mesencephalon+diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #this needs to be before D+S
sheetfromtxt$Anatomy <- sub("^(.*)BD$", "Body_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)BR$", "whole_brain_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)CX$", "Cerebral_Cortex_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)HP$", "hippocampus_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)CB$", "Cerebellum_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)ROB$", "RoB_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)D\\+S$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)D\\s*\\+\\s*S$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #change those with spacing errors as well
sheetfromtxt$Anatomy <- sub("^(.*)D\\+S\\s*$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #change those with spacing errors as well
sheetfromtxt$Anatomy <- sub("^(.*)MES$", "mesencephalon_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)P\\+M$", "pons+medulla_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)OB$", "Olfactory_bulb_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)GM$", "Cerebral_Cortex_Grey_Matter_\\1", sheetfromtxt$Anatomy)
# Replace specific cellular composition part of strings in the "Anatomy" column
sheetfromtxt$Anatomy <- sub("_M$", "_Mass_g", sheetfromtxt$Anatomy) #Mass (g)
sheetfromtxt$Anatomy <- sub("_N$", "_N_n", sheetfromtxt$Anatomy) #number of neurons
sheetfromtxt$Anatomy <- sub("_DN$", "_Nmg", sheetfromtxt$Anatomy) #neuronal density (in neurons/mg)
sheetfromtxt$Anatomy <- sub("_O/N$", "_ON", sheetfromtxt$Anatomy) #O/N, ratio between numbers of other (non-neuronal) cells and neurons
# Turn off scientific notation
# options(scipen=999)
# transpose the dataframe to a matrix
m <- t(sheetfromtxt)
# convert from matrix to dataframe
sheetfromtxt <- as.data.frame(m)
# Set column names to values in the first row of matrix
colnames(sheetfromtxt) <- m[1, ]
# Delete the first row of a dataframe by subsetting it to exclude the first row.
sheetfromtxt <- sheetfromtxt[-1, , drop = FALSE]
# Create a new column 'Species' with values copied from row names
sheetfromtxt$Species <- rownames(sheetfromtxt)
# Reorder the columns with "Species" as the first column
sheetfromtxt <- sheetfromtxt[, c("Species", setdiff(names(sheetfromtxt), "Species"))]
# Add a new column with row numbers
sheetfromtxt$Row_Numbers <- 1:nrow(sheetfromtxt)
# Replace rownames (species names) with Row_numbers
rownames(sheetfromtxt) <- sheetfromtxt$Row_Numbers
# Delete the "Row_numbers" column using subset
sheetfromtxt <- subset(sheetfromtxt, select = -Row_Numbers)
# Loop through the columns
# To convert all values to numeric, except for the first column (Species),
for (col in names(sheetfromtxt)) {
if (col != "Species") {
sheetfromtxt[[col]] <- as.numeric(sheetfromtxt[[col]])
}
}
## PART FOUR: Finalizing: delete redundant or unnecessary information
# Delete the row Delta
sheetfromtxt <- sheetfromtxt[!sheetfromtxt$Species == "delta", ]
# PART FIVE: give meaningful species names
# rename species so names are complete
sheetfromtxt$Species[sheetfromtxt$Species == "Marmosops"] <- "Marmosops_incanus"
sheetfromtxt$Species[sheetfromtxt$Species == "Metachirus"] <- "Metachirus_nudicaudatus"
sheetfromtxt$Species[sheetfromtxt$Species == "Didelphis"] <- "Didelphis_aurita"
sheetfromtxt$Species[sheetfromtxt$Species == "Sarcophilus"] <- "Sarcophilus_harrisii"
sheetfromtxt$Species[sheetfromtxt$Species == "Wallabia"] <- "Wallabia_bicolor"
sheetfromtxt$Species[sheetfromtxt$Species == "Dendrolagus"] <- "Dendrolagus_goodfellowi"
## PART SIX: Save
# Save it as 'dossantos_etal_2017.csv'
write.csv(sheetfromtxt, file = "dossantos_etal_2017.csv", row.names = FALSE)
# Original file:
# Dos Santos, S. E., Porfirio, J., da Cunha, F. B., Manger, P. R., Tavares, W., Pessoa, L., Raghanti, M. A., Sherwood, C. C., & Herculano-Houzel, S. (2017). Cellular Scaling Rules for the Brains of Marsupials: Not as "Primitive" as Expected. Brain Behav Evol. https://doi.org/10.1159/000452856
# Supplementary Material online
# link to figshare
# 1. "Downloaded all" from figshare link:
# https://figshare.com/articles/journal_contribution/Supplementary_Material_for_Cellular_Scaling_Rules_for_the_Brains_of_Marsupials_Not_as_Primitive_as_Expected/4588471?file=7428211
# 2. Unzip zipped file 4588471.zip (double click it)
# 3. Open 4588471/452856_sm10.doc in MS Word (double click it)
# 4. Save as Plain Text (.txt)
## PART ONE: BASIC FORMATTING
# Set Working Directory. Store with the spreadsheet.
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data/Cell_Counts_Isotropic_Fractionator/DosSantos_etal_2017")
#Load readr
library(readr)
# Read the text version
sheetfromtxt <- read_delim("452856_sm10.txt",
delim = "\t", escape_double = FALSE,
trim_ws = TRUE, skip = 1)
View(sheetfromtxt)
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(sheetfromtxt) = c(names(sheetfromtxt)[1:7],  names(sheetfromtxt)[9:length(names(sheetfromtxt))])
# Delete empty column. note 1a: you need to run it an extra time or use a similar functions, for some reason, so this is repeated below -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# rename columns
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...1"] ="Anatomy"
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...13"] ="delta"
# remove unnecessary character in a specific column -- this step maybe not needed?
sheetfromtxt$delta = gsub("x", "", sheetfromtxt$delta)
# remove unnecessary string in the whole dataset
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub("n.a.","",as.character(x))))
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(sheetfromtxt) = gsub("\\b..\\b", "_", colnames(sheetfromtxt))
## PART TWO: Correct the erratum
# "The correct number of neurons in the cerebral cortex (NCX) of the Tasmanian devil (Sarcophilus) in online supplementary Table S1 is 71.66 × 106"
# change [13,6] from 122.60x106 to 71.66×106
# change
# first check which cell it is
Sarcophilus_NCX_1 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_1)
# then change it to the corrected value
sheetfromtxt[13, "Sarcophilus"] <- 71.66e6
# sheetfromtxt[13, "Sarcophilus"] <- "71.66×106" #note: this is not working although it should. See below PART THREE note 2a --- !!!
# check the change worked
Sarcophilus_NCX_2 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_2)
## PART THREE: Rename variables and calculate data to make it usable
# rename string in columns (Genus)
colnames(sheetfromtxt) = gsub("M_", "Macropus_", colnames(sheetfromtxt))
# Convert equation string to a number (Steps 1_2)
# Step 1: load tidyverse
library(tidyverse)
# # Step 2: look for columns TRUE for 106 or 109. If true, need to multiply equation.
# for (i in 2:length(names(sheetfromtxt))) {
#   sheetfromtxt$x106=str_detect(sheetfromtxt[,i], 'x106')
#   sheetfromtxt$x109=str_detect(sheetfromtxt[,i], 'x109')
#   ifelse (sheetfromtxt$x106 == TRUE, sheetfromtxt[,i] <- gsub("x106", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, sheetfromtxt[,i] <- gsub("x109", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   sheetfromtxt[,i] <- as.numeric(sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x106 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^6), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^9), sheetfromtxt[,i])
# }
# # Remove the variable 'i' from the environment
# rm(i)
# sheetfromtxt$x106 = NULL
# sheetfromtxt$x109 = NULL
#note2b Would it be possible to instead of the i value above, just do a find and replace (which works in Excel), and replace all "10x" with "e" --- !!!
#note1b: get rid of extra line -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# Assuming your data frame is named 'speciesfromtxt'
for (row in 1:nrow(speciesfromtxt)) {
for (col in 1:ncol(speciesfromtxt)) {
if (grepl("x106", speciesfromtxt[row, col], fixed = TRUE)) {
speciesfromtxt[row, col] <- gsub("x106", "", speciesfromtxt[row, col])
speciesfromtxt[row, col] <- as.numeric(speciesfromtxt[row, col]) * 10^6
}
}
}
# Original file:
# Dos Santos, S. E., Porfirio, J., da Cunha, F. B., Manger, P. R., Tavares, W., Pessoa, L., Raghanti, M. A., Sherwood, C. C., & Herculano-Houzel, S. (2017). Cellular Scaling Rules for the Brains of Marsupials: Not as "Primitive" as Expected. Brain Behav Evol. https://doi.org/10.1159/000452856
# Supplementary Material online
# link to figshare
# 1. "Downloaded all" from figshare link:
# https://figshare.com/articles/journal_contribution/Supplementary_Material_for_Cellular_Scaling_Rules_for_the_Brains_of_Marsupials_Not_as_Primitive_as_Expected/4588471?file=7428211
# 2. Unzip zipped file 4588471.zip (double click it)
# 3. Open 4588471/452856_sm10.doc in MS Word (double click it)
# 4. Save as Plain Text (.txt)
## PART ONE: BASIC FORMATTING
# Set Working Directory. Store with the spreadsheet.
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data/Cell_Counts_Isotropic_Fractionator/DosSantos_etal_2017")
#Load readr
library(readr)
# Read the text version
sheetfromtxt <- read_delim("452856_sm10.txt",
delim = "\t", escape_double = FALSE,
trim_ws = TRUE, skip = 1)
View(sheetfromtxt)
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(sheetfromtxt) = c(names(sheetfromtxt)[1:7],  names(sheetfromtxt)[9:length(names(sheetfromtxt))])
# Delete empty column. note 1a: you need to run it an extra time or use a similar functions, for some reason, so this is repeated below -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# rename columns
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...1"] ="Anatomy"
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...13"] ="delta"
# remove unnecessary character in a specific column -- this step maybe not needed?
sheetfromtxt$delta = gsub("x", "", sheetfromtxt$delta)
# remove unnecessary string in the whole dataset
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub("n.a.","",as.character(x))))
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(sheetfromtxt) = gsub("\\b..\\b", "_", colnames(sheetfromtxt))
## PART TWO: Correct the erratum
# "The correct number of neurons in the cerebral cortex (NCX) of the Tasmanian devil (Sarcophilus) in online supplementary Table S1 is 71.66 × 106"
# change [13,6] from 122.60x106 to 71.66×106
# change
# first check which cell it is
Sarcophilus_NCX_1 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_1)
# then change it to the corrected value
sheetfromtxt[13, "Sarcophilus"] <- 71.66e6
# sheetfromtxt[13, "Sarcophilus"] <- "71.66×106" #note: this is not working although it should. See below PART THREE note 2a --- !!!
# check the change worked
Sarcophilus_NCX_2 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_2)
## PART THREE: Rename variables and calculate data to make it usable
# rename string in columns (Genus)
colnames(sheetfromtxt) = gsub("M_", "Macropus_", colnames(sheetfromtxt))
# Convert equation string to a number (Steps 1_2)
# Step 1: load tidyverse
library(tidyverse)
# # Step 2: look for columns TRUE for 106 or 109. If true, need to multiply equation.
# for (i in 2:length(names(sheetfromtxt))) {
#   sheetfromtxt$x106=str_detect(sheetfromtxt[,i], 'x106')
#   sheetfromtxt$x109=str_detect(sheetfromtxt[,i], 'x109')
#   ifelse (sheetfromtxt$x106 == TRUE, sheetfromtxt[,i] <- gsub("x106", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, sheetfromtxt[,i] <- gsub("x109", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   sheetfromtxt[,i] <- as.numeric(sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x106 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^6), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^9), sheetfromtxt[,i])
# }
# # Remove the variable 'i' from the environment
# rm(i)
# sheetfromtxt$x106 = NULL
# sheetfromtxt$x109 = NULL
#note2b Would it be possible to instead of the i value above, just do a find and replace (which works in Excel), and replace all "10x" with "e" --- !!!
#note1b: get rid of extra line -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# Assuming your data frame is named 'speciesfromtxt'
for (row in 1:nrow(sheetfromtxt)) {
for (col in 1:ncol(sheetfromtxt)) {
if (grepl("x106", sheetfromtxt[row, col], fixed = TRUE)) {
sheetfromtxt[row, col] <- gsub("x106", "", sheetfromtxt[row, col])
sheetfromtxt[row, col] <- as.numeric(sheetfromtxt[row, col]) * 10^6
}
}
}
# Replace specific brain component endings at the end of a string with the corresponding strings prepended in the "Anatomy" column
sheetfromtxt$Anatomy <- sub("^(.*)MES\\+D\\+S$", "mesencephalon+diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #this needs to be before D+S
sheetfromtxt$Anatomy <- sub("^(.*)BD$", "Body_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)BR$", "whole_brain_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)CX$", "Cerebral_Cortex_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)HP$", "hippocampus_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)CB$", "Cerebellum_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)ROB$", "RoB_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)D\\+S$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)D\\s*\\+\\s*S$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #change those with spacing errors as well
sheetfromtxt$Anatomy <- sub("^(.*)D\\+S\\s*$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #change those with spacing errors as well
sheetfromtxt$Anatomy <- sub("^(.*)MES$", "mesencephalon_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)P\\+M$", "pons+medulla_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)OB$", "Olfactory_bulb_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)GM$", "Cerebral_Cortex_Grey_Matter_\\1", sheetfromtxt$Anatomy)
# Replace specific cellular composition part of strings in the "Anatomy" column
sheetfromtxt$Anatomy <- sub("_M$", "_Mass_g", sheetfromtxt$Anatomy) #Mass (g)
sheetfromtxt$Anatomy <- sub("_N$", "_N_n", sheetfromtxt$Anatomy) #number of neurons
sheetfromtxt$Anatomy <- sub("_DN$", "_Nmg", sheetfromtxt$Anatomy) #neuronal density (in neurons/mg)
sheetfromtxt$Anatomy <- sub("_O/N$", "_ON", sheetfromtxt$Anatomy) #O/N, ratio between numbers of other (non-neuronal) cells and neurons
# Turn off scientific notation
# options(scipen=999)
# transpose the dataframe to a matrix
m <- t(sheetfromtxt)
# convert from matrix to dataframe
sheetfromtxt <- as.data.frame(m)
# Set column names to values in the first row of matrix
colnames(sheetfromtxt) <- m[1, ]
# Delete the first row of a dataframe by subsetting it to exclude the first row.
sheetfromtxt <- sheetfromtxt[-1, , drop = FALSE]
# Create a new column 'Species' with values copied from row names
sheetfromtxt$Species <- rownames(sheetfromtxt)
# Reorder the columns with "Species" as the first column
sheetfromtxt <- sheetfromtxt[, c("Species", setdiff(names(sheetfromtxt), "Species"))]
# Add a new column with row numbers
sheetfromtxt$Row_Numbers <- 1:nrow(sheetfromtxt)
# Replace rownames (species names) with Row_numbers
rownames(sheetfromtxt) <- sheetfromtxt$Row_Numbers
# Delete the "Row_numbers" column using subset
sheetfromtxt <- subset(sheetfromtxt, select = -Row_Numbers)
# Loop through the columns
# To convert all values to numeric, except for the first column (Species),
for (col in names(sheetfromtxt)) {
if (col != "Species") {
sheetfromtxt[[col]] <- as.numeric(sheetfromtxt[[col]])
}
}
## PART FOUR: Finalizing: delete redundant or unnecessary information
# Delete the row Delta
sheetfromtxt <- sheetfromtxt[!sheetfromtxt$Species == "delta", ]
# PART FIVE: give meaningful species names
# rename species so names are complete
sheetfromtxt$Species[sheetfromtxt$Species == "Marmosops"] <- "Marmosops_incanus"
sheetfromtxt$Species[sheetfromtxt$Species == "Metachirus"] <- "Metachirus_nudicaudatus"
sheetfromtxt$Species[sheetfromtxt$Species == "Didelphis"] <- "Didelphis_aurita"
sheetfromtxt$Species[sheetfromtxt$Species == "Sarcophilus"] <- "Sarcophilus_harrisii"
sheetfromtxt$Species[sheetfromtxt$Species == "Wallabia"] <- "Wallabia_bicolor"
sheetfromtxt$Species[sheetfromtxt$Species == "Dendrolagus"] <- "Dendrolagus_goodfellowi"
## PART SIX: Save
# Save it as 'dossantos_etal_2017.csv'
write.csv(sheetfromtxt, file = "dossantos_etal_2017.csv", row.names = FALSE)
# Original file:
# Dos Santos, S. E., Porfirio, J., da Cunha, F. B., Manger, P. R., Tavares, W., Pessoa, L., Raghanti, M. A., Sherwood, C. C., & Herculano-Houzel, S. (2017). Cellular Scaling Rules for the Brains of Marsupials: Not as "Primitive" as Expected. Brain Behav Evol. https://doi.org/10.1159/000452856
# Supplementary Material online
# link to figshare
# 1. "Downloaded all" from figshare link:
# https://figshare.com/articles/journal_contribution/Supplementary_Material_for_Cellular_Scaling_Rules_for_the_Brains_of_Marsupials_Not_as_Primitive_as_Expected/4588471?file=7428211
# 2. Unzip zipped file 4588471.zip (double click it)
# 3. Open 4588471/452856_sm10.doc in MS Word (double click it)
# 4. Save as Plain Text (.txt)
## PART ONE: BASIC FORMATTING
# Set Working Directory. Store with the spreadsheet.
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data/Cell_Counts_Isotropic_Fractionator/DosSantos_etal_2017")
#Load readr
library(readr)
# Read the text version
sheetfromtxt <- read_delim("452856_sm10.txt",
delim = "\t", escape_double = FALSE,
trim_ws = TRUE, skip = 1)
View(sheetfromtxt)
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(sheetfromtxt) = c(names(sheetfromtxt)[1:7],  names(sheetfromtxt)[9:length(names(sheetfromtxt))])
# Delete empty column. note 1a: you need to run it an extra time or use a similar functions, for some reason, so this is repeated below -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# rename columns
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...1"] ="Anatomy"
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...13"] ="delta"
# remove unnecessary character in a specific column -- this step maybe not needed?
sheetfromtxt$delta = gsub("x", "", sheetfromtxt$delta)
# remove unnecessary string in the whole dataset
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub("n.a.","",as.character(x))))
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(sheetfromtxt) = gsub("\\b..\\b", "_", colnames(sheetfromtxt))
## PART TWO: Correct the erratum
# "The correct number of neurons in the cerebral cortex (NCX) of the Tasmanian devil (Sarcophilus) in online supplementary Table S1 is 71.66 × 106"
# change [13,6] from 122.60x106 to 71.66×106
# change
# first check which cell it is
Sarcophilus_NCX_1 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_1)
# then change it to the corrected value
sheetfromtxt[13, "Sarcophilus"] <- 71.66e6
# sheetfromtxt[13, "Sarcophilus"] <- "71.66×106" #note: this is not working although it should. See below PART THREE note 2a --- !!!
# check the change worked
Sarcophilus_NCX_2 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_2)
## PART THREE: Rename variables and calculate data to make it usable
# rename string in columns (Genus)
colnames(sheetfromtxt) = gsub("M_", "Macropus_", colnames(sheetfromtxt))
# Convert equation string to a number (Steps 1_2)
# Step 1: load tidyverse
library(tidyverse)
# # Step 2: look for columns TRUE for 106 or 109. If true, need to multiply equation.
# for (i in 2:length(names(sheetfromtxt))) {
#   sheetfromtxt$x106=str_detect(sheetfromtxt[,i], 'x106')
#   sheetfromtxt$x109=str_detect(sheetfromtxt[,i], 'x109')
#   ifelse (sheetfromtxt$x106 == TRUE, sheetfromtxt[,i] <- gsub("x106", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, sheetfromtxt[,i] <- gsub("x109", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   sheetfromtxt[,i] <- as.numeric(sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x106 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^6), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^9), sheetfromtxt[,i])
# }
# # Remove the variable 'i' from the environment
# rm(i)
# sheetfromtxt$x106 = NULL
# sheetfromtxt$x109 = NULL
#note2b Would it be possible to instead of the i value above, just do a find and replace (which works in Excel), and replace all "10x" with "e" --- !!!
#note1b: get rid of extra line -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# Assuming your data frame is named 'speciesfromtxt'
for (row in 1:nrow(sheetfromtxt)) {
for (col in 1:ncol(sheetfromtxt)) {
if (grepl("x106", sheetfromtxt[row, col], fixed = TRUE)) {
sheetfromtxt[row, col] <- gsub("x106", "", sheetfromtxt[row, col])
sheetfromtxt[row, col] <- as.numeric(sheetfromtxt[row, col]) * 10^6
}
}
}
for (row in 1:nrow(sheetfromtxt)) {
for (col in 1:ncol(sheetfromtxt)) {
if (grepl("x109", sheetfromtxt[row, col], fixed = TRUE)) {
sheetfromtxt[row, col] <- gsub("x109", "", sheetfromtxt[row, col])
sheetfromtxt[row, col] <- as.numeric(sheetfromtxt[row, col]) * 10^9
}
}
}
# Replace specific brain component endings at the end of a string with the corresponding strings prepended in the "Anatomy" column
sheetfromtxt$Anatomy <- sub("^(.*)MES\\+D\\+S$", "mesencephalon+diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #this needs to be before D+S
sheetfromtxt$Anatomy <- sub("^(.*)BD$", "Body_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)BR$", "whole_brain_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)CX$", "Cerebral_Cortex_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)HP$", "hippocampus_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)CB$", "Cerebellum_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)ROB$", "RoB_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)D\\+S$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)D\\s*\\+\\s*S$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #change those with spacing errors as well
sheetfromtxt$Anatomy <- sub("^(.*)D\\+S\\s*$", "diencephalon+striatum_\\1", sheetfromtxt$Anatomy) #change those with spacing errors as well
sheetfromtxt$Anatomy <- sub("^(.*)MES$", "mesencephalon_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)P\\+M$", "pons+medulla_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)OB$", "Olfactory_bulb_\\1", sheetfromtxt$Anatomy)
sheetfromtxt$Anatomy <- sub("^(.*)GM$", "Cerebral_Cortex_Grey_Matter_\\1", sheetfromtxt$Anatomy)
# Replace specific cellular composition part of strings in the "Anatomy" column
sheetfromtxt$Anatomy <- sub("_M$", "_Mass_g", sheetfromtxt$Anatomy) #Mass (g)
sheetfromtxt$Anatomy <- sub("_N$", "_N_n", sheetfromtxt$Anatomy) #number of neurons
sheetfromtxt$Anatomy <- sub("_DN$", "_Nmg", sheetfromtxt$Anatomy) #neuronal density (in neurons/mg)
sheetfromtxt$Anatomy <- sub("_O/N$", "_ON", sheetfromtxt$Anatomy) #O/N, ratio between numbers of other (non-neuronal) cells and neurons
# Turn off scientific notation
# options(scipen=999)
# transpose the dataframe to a matrix
m <- t(sheetfromtxt)
# convert from matrix to dataframe
sheetfromtxt <- as.data.frame(m)
# Set column names to values in the first row of matrix
colnames(sheetfromtxt) <- m[1, ]
# Delete the first row of a dataframe by subsetting it to exclude the first row.
sheetfromtxt <- sheetfromtxt[-1, , drop = FALSE]
# Create a new column 'Species' with values copied from row names
sheetfromtxt$Species <- rownames(sheetfromtxt)
# Reorder the columns with "Species" as the first column
sheetfromtxt <- sheetfromtxt[, c("Species", setdiff(names(sheetfromtxt), "Species"))]
# Add a new column with row numbers
sheetfromtxt$Row_Numbers <- 1:nrow(sheetfromtxt)
# Replace rownames (species names) with Row_numbers
rownames(sheetfromtxt) <- sheetfromtxt$Row_Numbers
# Delete the "Row_numbers" column using subset
sheetfromtxt <- subset(sheetfromtxt, select = -Row_Numbers)
# Loop through the columns
# To convert all values to numeric, except for the first column (Species),
for (col in names(sheetfromtxt)) {
if (col != "Species") {
sheetfromtxt[[col]] <- as.numeric(sheetfromtxt[[col]])
}
}
## PART FOUR: Finalizing: delete redundant or unnecessary information
# Delete the row Delta
sheetfromtxt <- sheetfromtxt[!sheetfromtxt$Species == "delta", ]
# PART FIVE: give meaningful species names
# rename species so names are complete
sheetfromtxt$Species[sheetfromtxt$Species == "Marmosops"] <- "Marmosops_incanus"
sheetfromtxt$Species[sheetfromtxt$Species == "Metachirus"] <- "Metachirus_nudicaudatus"
sheetfromtxt$Species[sheetfromtxt$Species == "Didelphis"] <- "Didelphis_aurita"
sheetfromtxt$Species[sheetfromtxt$Species == "Sarcophilus"] <- "Sarcophilus_harrisii"
sheetfromtxt$Species[sheetfromtxt$Species == "Wallabia"] <- "Wallabia_bicolor"
sheetfromtxt$Species[sheetfromtxt$Species == "Dendrolagus"] <- "Dendrolagus_goodfellowi"
## PART SIX: Save
# Save it as 'dossantos_etal_2017.csv'
write.csv(sheetfromtxt, file = "dossantos_etal_2017.csv", row.names = FALSE)
## PART ONE: BASIC FORMATTING
# Set Working Directory. Store with the spreadsheet.
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data/Cell_Counts_Isotropic_Fractionator/DosSantos_etal_2017")
#Load readr
library(readr)
# Read the text version
sheetfromtxt <- read_delim("452856_sm10.txt",
delim = "\t", escape_double = FALSE,
trim_ws = TRUE, skip = 1)
View(sheetfromtxt)
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(sheetfromtxt) = c(names(sheetfromtxt)[1:7],  names(sheetfromtxt)[9:length(names(sheetfromtxt))])
# Delete empty column. note 1a: you need to run it an extra time or use a similar functions, for some reason, so this is repeated below -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# rename columns
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...1"] ="Anatomy"
colnames(sheetfromtxt)[colnames(sheetfromtxt) == "...13"] ="delta"
# remove unnecessary character in a specific column -- this step maybe not needed?
sheetfromtxt$delta = gsub("x", "", sheetfromtxt$delta)
# remove unnecessary string in the whole dataset
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub("n.a.","",as.character(x))))
sheetfromtxt <- data.frame(sapply(sheetfromtxt,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(sheetfromtxt) = gsub("\\b..\\b", "_", colnames(sheetfromtxt))
## PART TWO: Correct the erratum
# "The correct number of neurons in the cerebral cortex (NCX) of the Tasmanian devil (Sarcophilus) in online supplementary Table S1 is 71.66 × 106"
# change [13,6] from 122.60x106 to 71.66×106
# change
# first check which cell it is
Sarcophilus_NCX_1 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_1)
# then change it to the corrected value
sheetfromtxt[13, "Sarcophilus"] <- 71.66e6
# sheetfromtxt[13, "Sarcophilus"] <- "71.66×106" #note: this is not working although it should. See below PART THREE note 2a --- !!!
# check the change worked
Sarcophilus_NCX_2 <- sheetfromtxt[13, "Sarcophilus"]
print(Sarcophilus_NCX_2)
## PART THREE: Rename variables and calculate data to make it usable
# rename string in columns (Genus)
colnames(sheetfromtxt) = gsub("M_", "Macropus_", colnames(sheetfromtxt))
# Convert equation string to a number (Steps 1_2)
# Step 1: load tidyverse
library(tidyverse)
# # Step 2: look for columns TRUE for 106 or 109. If true, need to multiply equation.
# for (i in 2:length(names(sheetfromtxt))) {
#   sheetfromtxt$x106=str_detect(sheetfromtxt[,i], 'x106')
#   sheetfromtxt$x109=str_detect(sheetfromtxt[,i], 'x109')
#   ifelse (sheetfromtxt$x106 == TRUE, sheetfromtxt[,i] <- gsub("x106", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, sheetfromtxt[,i] <- gsub("x109", "", sheetfromtxt[,i]), sheetfromtxt[,i])
#   sheetfromtxt[,i] <- as.numeric(sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x106 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^6), sheetfromtxt[,i])
#   ifelse (sheetfromtxt$x109 == TRUE, as.numeric(sheetfromtxt[,i] <-  sheetfromtxt[,i]*10^9), sheetfromtxt[,i])
# }
# # Remove the variable 'i' from the environment
# rm(i)
# sheetfromtxt$x106 = NULL
# sheetfromtxt$x109 = NULL
#note2b Would it be possible to instead of the i value above, just do a find and replace (which works in Excel), and replace all "10x" with "e" --- !!!
#note1b: get rid of extra line -- !!!
sheetfromtxt[,13]<-NULL
sheetfromtxt=sheetfromtxt[,-13]
# Assuming your data frame is named 'speciesfromtxt'
for (row in 1:nrow(sheetfromtxt)) {
for (col in 1:ncol(sheetfromtxt)) {
if (grepl("x106", sheetfromtxt[row, col], fixed = TRUE)) {
sheetfromtxt[row, col] <- gsub("x106", "", sheetfromtxt[row, col])
sheetfromtxt[row, col] <- as.numeric(sheetfromtxt[row, col]) * 10^6
}
}
}
for (row in 1:nrow(sheetfromtxt)) {
for (col in 1:ncol(sheetfromtxt)) {
if (grepl("x109", sheetfromtxt[row, col], fixed = TRUE)) {
sheetfromtxt[row, col] <- gsub("x109", "", sheetfromtxt[row, col])
sheetfromtxt[row, col] <- as.numeric(sheetfromtxt[row, col]) * 10^9
}
}
}
