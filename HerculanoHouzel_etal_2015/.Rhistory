)
}
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 2. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 3. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = " ±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
View(tabledirectxl)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 2. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 3. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub(" \\+ ", " ± ", tabledirectxl$`N, n`["Rattus norvegicus"])
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub(" + ", " ± ", tabledirectxl$`N, n`["Rattus norvegicus"])
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 3. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# in column "N,n" row "Rattus norvegicus" change " + " to " ± "
# Replace " + " with " ± " in the specified column and row
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub(" + ", " ± ", tabledirectxl$`N, n`["Rattus norvegicus"])
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub("+", "±", tabledirectxl$`N, n`["Rattus norvegicus"])
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub(" \\+ ", " ± ", x))
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 3. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# in column "N,n" row "Rattus norvegicus" change " + " to " ± "
# Replace " + " with " ± " in the specified column and row
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub("+", "±", tabledirectxl$`N, n`["Rattus norvegicus"])
rownames(tabledirectxl) <- NULL
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# Reindex the rows
rownames(tabledirectxl) <- NULL
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# Reindex the rows
rownames(tabledirectxl) <- NULL
# Replace " + " with " ± " in the specified column and row
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub("+", "±", tabledirectxl$`N, n`["Rattus norvegicus"])
rownames(tabledirectxl) <- 1:(nrow(tabledirectxl))
tabledirectxl$`N, n`["Rattus norvegicus"] <- gsub("+", "±", tabledirectxl$`N, n`["Rattus norvegicus"])
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# Reindex the rows
rownames(tabledirectxl) <- 1:(nrow(tabledirectxl))
## 4. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub(" + ", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub(" + ", " ± ", x))
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("+", "±", x))
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# Reindex the rows
rownames(tabledirectxl) <- 1:(nrow(tabledirectxl))
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("+", "±", x))
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct possible error in symbol
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric
columns_to_convert <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(tabledirectxl[[column]])
}
View(tabledirectxl)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric
# columns_to_convert <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
#
# for (column in columns_to_convert) {
#   tabledirectxl[[column]] <- as.numeric(tabledirectxl[[column]])
# }
columns_to_convert <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 6. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass", "Mass SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
tabledirectxl
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 6. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
View(tabledirectxl)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 6. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 7. Save as csv
# Save the dataframe to a CSV file
write.csv(merged_df, file = "HerculanoHouzel_etal_2015_Table1.csv", row.names = FALSE)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 6. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 7. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table1.csv", row.names = FALSE)
## 8. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table1.tsv", row.names = FALSE)
