columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 7. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table1.csv", row.names = FALSE)
## 8. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table1.tsv", row.names = FALSE)
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
## 2. Delete empty columns
tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
View(tabledirectxl)
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
View(tabledirectxl)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data/HerculanoHouzel_etal_2015/HerculanoHouzel_etal_2015_Table1")
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
View(tabledirectxl)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
# ## 2. Delete empty columns
# tabledirectxl <- tabledirectxl[, colSums(is.na(tabledirectxl) | tabledirectxl == "") != nrow(tabledirectxl), drop = FALSE]
#
## 3. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 4. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 5. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 6. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 7. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table1.csv", row.names = FALSE)
## 8. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table1.tsv", row.names = FALSE)
View(tabledirectxl)
# Table 1
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table1.xlsx")
## 2. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
## 3. Correct (possible) error
# There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# Replace " + " with " ± " in the entire dataset
tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 6. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table1.csv", row.names = FALSE)
## 7. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table1.tsv", row.names = FALSE)
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data/HerculanoHouzel_etal_2015")
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
## 2. Table name / header removal
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
View(tabledirectxl)
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
View(tabledirectxl)
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 6. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table2.csv", row.names = FALSE)
## 7. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table2.tsv", row.names = FALSE)
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# ## 3. Correct (possible) error
# # There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# # Replace " + " with " ± " in the entire dataset
# tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Check if columns to split exist in the dataframe
missing_cols <- setdiff(cols_to_split, colnames(tabledirectxl))
if (length(missing_cols) > 0) {
stop(paste("The following columns to split do not exist in the dataframe:", paste(missing_cols, collapse = ", ")))
}
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# ## 3. Correct (possible) error
# # There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# # Replace " + " with " ± " in the entire dataset
# tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g1", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
#
# # Check if columns to split exist in the dataframe
# missing_cols <- setdiff(cols_to_split, colnames(tabledirectxl))
# if (length(missing_cols) > 0) {
#   stop(paste("The following columns to split do not exist in the dataframe:", paste(missing_cols, collapse = ", ")))
# }
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
# Table 2
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table2.xlsx")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# ## 3. Correct (possible) error
# # There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# # Replace " + " with " ± " in the entire dataset
# tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 6. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table2.csv", row.names = FALSE)
## 7. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table2.tsv", row.names = FALSE)
# Table 3
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table3.xlsx")
View(tabledirectxl)
# Table 3
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table3.xlsx")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# ## 3. Correct (possible) error
# # There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# # Replace " + " with " ± " in the entire dataset
# tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 6. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table3.csv", row.names = FALSE)
## 7. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table3.tsv", row.names = FALSE)
# Table 3
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table3.xlsx")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# ## 3. Correct (possible) error
# # There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# # Replace " + " with " ± " in the entire dataset
# tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 6. Name columns after the structure, RoB
# Specify the columns to rename
columns_to_rename <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
# Add "RoB " at the beginning of each column name
colnames(tabledirectxl)[match(columns_to_rename, colnames(tabledirectxl))] <- paste0("RoB ", columns_to_rename)
# Table 3
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table3.xlsx")
View(tabledirectxl)
# Table 3
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table3.xlsx")
## 1. Check Table name
# Assuming the first column header is "Table 3 RoB"
first_column_name <- colnames(tabledirectxl)[1]
# Specify the prefix to remove
prefix_to_remove <- "Table 3"
# Extract the part after the prefix
variable_name <- sub(paste0("^", prefix_to_remove, "\\s+"), "", first_column_name)
# Print the extracted variable name
cat("Extracted variable name:", variable_name, "\n")
## 1. Check Table name
# Assuming the first column header is "Table 3 RoB"
first_column_name <- colnames(tabledirectxl)[1]
# Specify the prefix to remove
prefix_to_remove <- "Table 3. "
# Extract the part after the prefix
variable_name <- sub(paste0("^", prefix_to_remove, "\\s+"), "", first_column_name)
# Print the extracted variable name
cat("Extracted variable name:", variable_name, "\n")
## 1. Check Table name
# Assuming the first column header is "Table 3. RoB"
first_column_name <- colnames(tabledirectxl)[1]
# Specify the prefix to remove
prefix_to_remove <- "Table 3. "
# Extract the part after the prefix
variable_name <- sub(paste0("^", prefix_to_remove, "\\s+"), "", first_column_name)
# Print the extracted variable name
cat("Extracted variable name:", variable_name, "\n")
# Assuming the first column header is "Table 3. RoB"
first_column_name <- colnames(tabledirectxl)[1]
# Specify the prefix to remove
prefix_to_remove <- "Table 3. "
# Extract the part after the prefix
variable_name <- sub(paste0("^", prefix_to_remove), "", first_column_name)
# Print the extracted variable name
cat("Extracted variable name:", variable_name, "\n")
# Table 3
## 1. Read direct from xl
library(readxl)
tabledirectxl <- read_excel("HerculanoHouzel_etal_2015_Table3.xlsx")
## 1. Check Table name
# Assuming the first column header is "Table 3. RoB"
first_column_name <- colnames(tabledirectxl)[1]
# Specify the prefix to remove
prefix_to_remove <- "Table 3. "
# Extract the part after the prefix
variable_name <- sub(paste0("^", prefix_to_remove), "", first_column_name)
# Print the extracted variable name
cat("Extracted variable name:", variable_name, "\n")
## 2. Remove table name header and bottom note
# Set the next row as the new header
colnames(tabledirectxl) <- as.character(unlist(tabledirectxl[1, ]))
# Remove the first row since it's now the header
tabledirectxl <- tabledirectxl[-1, ]
# Remove the last two rows which are notes
tabledirectxl <- tabledirectxl[1:(nrow(tabledirectxl)-2), ]
# ## 3. Correct (possible) error
# # There seems to be a " + " in place of " ± " in 	column "N,n" row "Rattus norvegicus"
# # Replace " + " with " ± " in the entire dataset
# tabledirectxl[] <- lapply(tabledirectxl, function(x) gsub("\\s*\\+\\s*", " ± ", x))
## 4. Split 6 columns containing average ± standard deviation into 12 different columns.
# Load the 'tidyr' package
library(tidyr)
# Define the columns to split and their corresponding new column names
cols_to_split <- c("Mass, g", "N, n", "O, n", "N/mg",	"O/mg",	"O/N")
new_col_names <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg",	"O/mg SD",	"O/N", "O/N SD")
# Loop through the columns and split each one
for (i in seq_along(cols_to_split)) {
tabledirectxl <- separate(
tabledirectxl,
col = cols_to_split[i],  # Specify the column to split
into = c(new_col_names[i * 2 - 1], paste0(new_col_names[i * 2 - 1], " SD")),  # New column names with space before SD
#sep = " ± ",  # Specify the separator as a regular expression to split on ' ±'
sep = "\\s*±\\s*",  # Specify the separator as a regular expression to split on ' ±' with or without space
extra = "drop"  # Drop any extra pieces
)
}
## 5. Convert columns to numeric (taking away commas)
columns_to_convert <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD")
for (column in columns_to_convert) {
tabledirectxl[[column]] <- as.numeric(gsub(",", "", tabledirectxl[[column]]))
}
## 6. Name columns after the structure, RoB
# Specify the columns to rename
columns_to_rename <- c("Mass, g", "Mass, g SD", "N, n", "N, n SD", "O, n", "O, n SD", "N/mg", "N/mg SD", "O/mg", "O/mg SD", "O/N", "O/N SD", "Source")
# Add "RoB " at the beginning of each column name
colnames(tabledirectxl)[match(columns_to_rename, colnames(tabledirectxl))] <- paste0(variable_name, " ", columns_to_rename)
## 6. Save as csv
# Save the dataframe to a CSV file
write.csv(tabledirectxl, file = "HerculanoHouzel_etal_2015_Table3.csv", row.names = FALSE)
## 7. Save as tsv with DOI file name
# Save the dataframe to a TSV file
write.csv(tabledirectxl, file = "10.1159%2F000437413_table3.tsv", row.names = FALSE)
