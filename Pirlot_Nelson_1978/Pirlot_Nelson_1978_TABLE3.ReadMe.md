# Pirlot_Nelson_1978_TABLE3

Table 3, absolute volumes of brain structures and percentages of total brain volume. Blank cells reflect structures not separately reported. Mesocortex is reported only for the opossum.

The snapshot preserves the published labels and values. The R script writes the local CSV and the DOI-coded public TSV named through `__ReadMe.xlsx`.
