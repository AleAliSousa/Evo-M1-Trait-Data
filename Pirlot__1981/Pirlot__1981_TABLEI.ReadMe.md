# Pirlot__1981_TABLEI

Table I, List of material. S = sectioned and used for component analysis and encephalization; W = weighed and used for encephalization only. Parentheses around 2200 are retained as printed.

The snapshot preserves the published labels and values. The R script writes the local CSV and the DOI-coded public TSV named through `__ReadMe.xlsx`.
