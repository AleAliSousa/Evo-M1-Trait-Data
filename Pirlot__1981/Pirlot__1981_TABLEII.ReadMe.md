# Pirlot__1981_TABLEII

Table II, actual volumes in cubic mm for 12 brain components. The paper also prints total brain and RH+PY summary rows; these are derivable and are not stored as independent measurements.

The snapshot preserves the published labels and values. The R script writes the local CSV and the DOI-coded public TSV named through `__ReadMe.xlsx`.
