# R twins vs Python builders — 2026-08-07 10:20

Generated by `_checks/compare_R_vs_python_builders.py`. For each folder that carries both
a `*_compiled.R` and a `build_*_merge.py`, the R outputs (as written by the last
`run_all_scripts_v2.R` sweep) were snapshotted, the Python builder re-run over them, the
results compared cell-by-cell, and the R outputs restored. The repo is left unchanged.

| Folder | Python | R twin | R in sweep | Verdict |
|---|---|---|---|---|
| `__merging_body_ecology` | `build_body_ecology_merge.py` | `body_ecology_compiled.R` | SUCCESS | **PY_ABSENT** |
| `__merging_brain_mass` | `build_brain_mass_merge.py` | `brain_mass_compiled.R` | FAILED | **PY_ABSENT** |
| `__merging_cerebral_metabolic_rate` | `build_cerebral_metabolic_rate_merge.py` | `cerebral_metabolic_rate_compiled.R` | SUCCESS | **PY_ABSENT** |
| `__merging_fossil_brain_glucose` | `build_fossil_brain_glucose_merge.py` | `fossil_brain_glucose_compiled.R` | SUCCESS | **PY_ABSENT** |
| `__merging_sleep` | `build_sleep_merge.py` | `sleep_compiled.R` | SUCCESS | **PY_ABSENT** |
| `__merging_volumes` | `_verify_volumes_compiled_select.py` | `volumes_compiled_select.R` | SUCCESS | **PY_ABSENT** |
| `__merging_volumes` | `_verify_species_dtype.py` | `volumes_compiled.R` | SUCCESS | **PY_ABSENT** |
| `__ShinyApp` | `build_data.py` | `build_data.R` | SUCCESS | **PY_ABSENT** |

## Verdicts

- **IDENTICAL** — R reproduces the Python cell-for-cell (missing markers normalised: R `NA` == Python empty field); the `.py` is redundant and safe to delete.
- **IDENTICAL_TO_ROUNDING** — same data; a few cells differ only in the last printed decimal (≤ `DISPLAY_TOL`). Your call whether that counts as agreement.
- **NO_OUTPUT** — the `.py` produces no data file of its own (a diagnostic or a stub); judge it on whether it still runs.
- **PY_CANNOT_RUN** — the `.py` already fails (usually a hardcoded `/sessions/…` path from the session that wrote it), so the R twin is the only live implementation.
- **DIFFERS** — the two do NOT agree. Do not delete; reconcile first.
- **R_FAILED** — the sweep could not run the R twin, so the Python is currently load-bearing. Keep it and fix the R.

## Detail

### `__merging_body_ecology/build_body_ecology_merge.py` — PY_ABSENT

already removed from the working tree

### `__merging_brain_mass/build_brain_mass_merge.py` — PY_ABSENT

already removed from the working tree

### `__merging_cerebral_metabolic_rate/build_cerebral_metabolic_rate_merge.py` — PY_ABSENT

already removed from the working tree

### `__merging_fossil_brain_glucose/build_fossil_brain_glucose_merge.py` — PY_ABSENT

already removed from the working tree

### `__merging_sleep/build_sleep_merge.py` — PY_ABSENT

already removed from the working tree

### `__merging_volumes/_verify_volumes_compiled_select.py` — PY_ABSENT

already removed from the working tree

### `__merging_volumes/_verify_species_dtype.py` — PY_ABSENT

diagnostic print-only port of volumes_compiled.R steps 1-3; already removed from the working tree

### `__ShinyApp/build_data.py` — PY_ABSENT

5-line stub whose own text says DEPRECATED - replaced by build_data.R; already removed from the working tree

